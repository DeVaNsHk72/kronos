# OpenAI (used by the chat endpoint for intent parsing + answer generation)
OPEN_AI_API_KEY=sk-...

# Optional: path to DERIVED_DATA if not at ./DERIVED_DATA
# DERIVED_DATA_DIR=

# Optional: root directory containing ripped PDFs (for /api/download)
# PAPERS_ROOT=
